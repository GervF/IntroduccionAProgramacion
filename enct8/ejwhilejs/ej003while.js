// 3. Suma de cifras.
// Ingresar un número de cualquier cifra y mostrar,
// la suma de sus cifras.

let suma = 0;
let contador = 1
let control = 10
let i = 1

let numero = prompt("Ingrese un numero de cualquier cifra y le digo la suma de las cifras")
while (numero < i ) {numero.toString().split('')}
